(++) 1. if/else..., not
  (++) not
  (++) if/else
(++) 2. is_number, is_integer, is_float
  (++) is_number
  (++) is_integer
  (++) is_float
(++) 3. [integer] sign, abs
  (++) sign
  (++) abs
(++) 4. [integer] compare
(++) 5. [integer] gt, lt, gte, lte, eq, neq
(++) 6. [integer] add, subtract, multiply, divide
  (++) add
  (++) subtract
  (++) multiply
  (++) divide
(++) 7. float add,subtract,multiply,divide
() 8. refactor on v3
